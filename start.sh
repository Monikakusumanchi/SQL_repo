# docker compose build
# docker compose up
# docker exec -it <container name> /bin/bash
# psql -U myuser -d mydatabase -p mypassword
# use <database name>
# select * from table;
# docker restart <container name>
# docker --force delete <container name>
# docker ps
# docker volume ls
